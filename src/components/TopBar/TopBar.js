const TopBar = () => {
  const style = {
    width: "100%",
    height: "50px",
    background: "#fff",
    boxShadow: "0px 4px 4px rgba(0, 0, 0, 0.25)",
  };
  return <header style={style}></header>;
};

export default TopBar;
